✅JavaScriptの分割代入は const{a,b}=関数 とあった場合 関数がオブジェクトを取得する関数である かつオブジェクトのプロパティ名がa,bである プロパティ名と変数名はおなじでないと 分割代入できない

🔹基本ルール
const { a, b } = 関数();

このとき：
関数() は オブジェクトを返す関数 である必要がある
オブジェクトの中に a と b というプロパティ が存在している必要がある
変数名とプロパティ名が 一致していなければ分割代入できない

※分割代入できた変数はJSX内にそのまま変数名を書けば値を出力できる